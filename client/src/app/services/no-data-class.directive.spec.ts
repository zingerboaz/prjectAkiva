import { NoDataClassDirective } from './no-data-class.directive';

describe('NoDataClassDirective', () => {
  it('should create an instance', () => {
    const directive = new NoDataClassDirective();
    expect(directive).toBeTruthy();
  });
});
